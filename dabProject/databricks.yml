# This is a Databricks asset bundle definition for dabProject.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: dabProject
  uuid: 0fb522b4-2cfb-4aff-92b7-a0c367c78543

include:
  - resources/*.yml
  - resources/*/*.yml


artifacts:
  python_artifact:
    type: whl
    build: uv build --wheel

# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    presets:
      name_prefix: dev_${workspace.current_user.short_name}
      source_linked_deployment: false
    workspace:
      host: https://dbc-e6f2395b-5670.cloud.databricks.com/
    variables:
      catalog: workspace
      schema: dev
  prod:
    mode: production
    presets:
      name_prefix: prod_${workspace.current_user.short_name}
      source_linked_deployment: false
    workspace:
      host: https://dbc-e6f2395b-5670.cloud.databricks.com/
      # We explicitly deploy to /Workspace/Users/ruparaja1990@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Users/ruparaja1990@gmail.com/prod/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: workspace
      schema: prod
    permissions:
      - user_name: ruparaja1990@gmail.com
        level: CAN_MANAGE
